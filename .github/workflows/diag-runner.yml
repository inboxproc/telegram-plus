name: Diag Runner Alloc

on:
  workflow_dispatch:

jobs:
  alloc-ubuntu:
    runs-on: ubuntu-latest
    steps:
      - name: ubuntu runner allocation test
        run: echo "ubuntu runner allocation test OK on $(uname -a)"
  alloc-macos:
    runs-on: macos-latest
    steps:
      - name: macos-latest runner allocation test
        run: echo "macos-latest runner allocation test OK on $(uname -a)"
  alloc-macos26:
    runs-on: macos-26
    steps:
      - name: macos-26 runner allocation test
        run: echo "macos-26 runner allocation test OK on $(uname -a)"
